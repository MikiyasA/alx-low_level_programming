#include "main.h"
#include <stdlib.h>

/**
 * _abs - print absolut value od No
 * @i: input number as an integer
 * Return: i
 */

int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
