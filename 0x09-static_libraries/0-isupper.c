#include "main.h"
#include <ctype.h>

/**
 * _isupper - to check letter is Alpha or not
 * @c: letter to be checked
 * Return: 1 amd 0
 */

int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
