#include <stdio.h>

/**
 * main - this code will prind whole alphabet using for
 * Description: using the main function
 * Return: o value
 */

int main(void)
{
char ch;

for (ch = 'a'; ch <= 'z'; ch++)
{
putchar (ch);

}
putchar ('\n');
return (0);
}
