#include <stdio.h>

/**
 * main - print a single digit
 * Discription: usign the main funtion
 * Return: 0 value
 */

int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
printf("%d", i);
}
putchar('\n');

return (0);
}
