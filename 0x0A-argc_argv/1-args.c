#include "main.h"
#include <stdio.h>

/**
 * main - No. of arguments passed in to it
 * @argc: No. of comand line
 * @argv: ary that contain the progrma command line
 * Return: o value
 */

int main(int argc, char *argv[] __attribute__((unused)))
{

printf("%d\n", argc - 1);
return (0);

}
